// Dados primitivos Js

const nome = 'Luiz';
const nome1 = "LuizF"; // String
const num1 = `janael`; // String template 
const numb1 = 1; // number
let nomeAluno; // undefined -> não aponta local nenhum da memoria
const sobreNome = null; // Nulo -> não aponta pra local nenhum da memória.
const boolean = false; // pode ser false ou true somente

console.log(typeof sobreNome); // Para ver o tipo da Variavel

// dados referencia

const a = [1,2];
const b = a;
